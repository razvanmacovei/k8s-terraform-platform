# Environment variables for Terraform Kubernetes Platform
# Copy this file to .env and replace the placeholder values with real secrets
#
# IMPORTANT: Never commit the .env file to version control!

# PostgreSQL credentials
POSTGRESQL_USERNAME=your_postgres_username
POSTGRESQL_PASSWORD=your_secure_password
POSTGRESQL_DATABASE=your_database_name
POSTGRESQL_REPLICATION_PASSWORD=your_replication_password

# Redis credentials (optional - leave empty if Redis auth is disabled)
REDIS_PASSWORD=your_redis_password
