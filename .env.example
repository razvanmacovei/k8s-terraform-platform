# Environment variables for Terraform Kubernetes Platform
# Copy this file to .env and replace the placeholder values with real secrets
#
# IMPORTANT: Never commit the .env file to version control!
# Variables must use TF_VAR_ prefix to be picked up by Terraform.

# PostgreSQL credentials (only needed when postgresql is enabled)
TF_VAR_POSTGRESQL_USERNAME=your_postgres_username
TF_VAR_POSTGRESQL_PASSWORD=your_secure_password
TF_VAR_POSTGRESQL_DATABASE=your_database_name
TF_VAR_POSTGRESQL_REPLICATION_PASSWORD=your_replication_password

# Redis credentials (optional - leave empty if Redis auth is disabled)
TF_VAR_REDIS_PASSWORD=your_redis_password
